[package]
name = "fork-choice"
version = "0.1.0"
edition = "2021"

[lib]
name = "fork_choice"
path = "src/lib.rs"

[dependencies]
containers = { path = "../containers" }
serde = { version = "1.0", features = ["derive"] }
hex = "0.4.3"
thiserror = "1.0"
